import [[project_name]].[[project_class]] as P
import pytest
import os
import logging

import smtplib
from mock import Mock
from mock import patch, call
from mock import MagicMock
from mock import PropertyMock


class Test[[project_name]]:

    def setup(self):
        self.p = P.[[project_class]]()

