#!/usr/bin/env zsh -i

# Auto-generated by Voodoo
# First-time script for project setup (DELETE ME AFTER RUNNING!)

DOCS_DIR=~/dev/githubdocs

print Setting up your virtualenv

rmvirtualenv [[project_name]]
if [ $? -ne 0 ]; then
    print Removing old virtualenv failed
    exit -1
fi
mkvirtualenv [[project_name]]
if [ $? -ne 0 ]; then
    print Making [[project_name]] Virtual env failed
    exit -1
fi

workon [[project_name]]
if [ $? -ne 0 ]; then
    print Could not switch to [[project_name]]
    exit -1
fi
print Working in virtualenv [[project_name]]


# Set up the pip packages
#pip install pytest mock pytest-cov python-coveralls coverage sphinx tox
pip install sphinx
echo "cd ~/dev/[[project_name]]" >> ~/dev/envs/[[project_name]]/bin/postactivate

# Start python develop
python setup.py develop

# Initialize the git repo
github_remote='git@github.com:[[github_username]]/[[project_name]].git'
git init
git remote add origin $github_remote
git add .
git commit -am "Setting up new project [[project_name]]"

# Prompt if we want to push to remote git
read -q "REPLY?Push to remote repository $github_remote [y/N]?"
if [[ '[[' ]]  $REPLY == y [[ ']]' ]]; then
    git push -u origin master
fi

print
# Create the docs repository
current_dir=`pwd`
read -q "REPLY?Create and push docs to $github_remote [y/N]?"
if [[ '[[' ]]  $REPLY == y [[ ']]' ]]; then
    # Go to the docs build dir, and check out our repo
    cd $DOCS_DIR
    git clone https://github.com/virantha/[[project_name]].git
    cd [[project_name]]
    git checkout --orphan gh-pages
    git rm -rf .

    cd $current_dir/docs
    pip install sphinx
    make html
    cd $DOCS_DIR
    cd [[project_name]]
    touch .nojekyll
    git add .
    git commit -m "docs"
    git push origin gh-pages

fi
